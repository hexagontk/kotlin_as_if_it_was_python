#!/usr/bin/env kotlin

/*
 * 1. SHEBANG
 *
 * The shebang makes the file a proper executable, so you can run it with: `./1_hello.main.kts`
 * (the file must be marked as executable before)
 */

println("Hello Kotlin scripting")
